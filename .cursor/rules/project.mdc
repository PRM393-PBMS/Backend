---
description: Repository context and backend skill routing for PRM393
alwaysApply: true
---

Read `AGENTS.md` for repository conventions. For backend implementation, debugging or review, read `.agents/skills/prm393-backend/SKILL.md` and apply the relevant sections.

Use npm with the existing lockfile. The stack is NestJS, TypeScript, Prisma and PostgreSQL. Preserve unrelated changes and existing dependency versions. Keep generated output and secrets out of edits. Report checks actually run.

Follow the additional-skill routing in `AGENTS.md`: NestJS best practices for framework changes, the local Prisma 6 reference for persistence, and security-best-practices for explicit security requests. Read only relevant supporting files. Project conventions and installed package versions take precedence over upstream examples.
